Topic row with its own progress (PRD: every topic tracks progress independently).

```jsx
<TopicCard title="זכות קדימה" subtitle="חלק 2 מתוך 3" value={64} max={100}
  image="assets/signs/sign-301-yield.png" onClick={open} />
```
